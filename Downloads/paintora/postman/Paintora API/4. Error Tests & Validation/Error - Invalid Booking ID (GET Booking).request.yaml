$kind: http-request
description: Tests 404 response for non-existent booking ID.
url: "{{base_url}}/bookings.php?id=99999"
method: GET
queryParams:
  id: "99999"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 404 Not Found", function () {
          pm.response.to.have.status(404);
      });
      pm.test("Error code is BOOKING_NOT_FOUND", function () {
          var json = pm.response.json();
          pm.expect(json.error).to.eql('BOOKING_NOT_FOUND');
      });
    language: text/javascript
order: 5000
